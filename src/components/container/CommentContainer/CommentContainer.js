import React, { Component } from 'react';

export default class CommentContainer extends Component {
    render() {
        return (
            <div>
                CommentContainer
            </div>

        )
    }
}